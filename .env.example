# Database
DATABASE_URL="postgresql://trecco:trecco_dev@localhost:5432/trecco"

# Auth
JWT_ACCESS_SECRET=""
JWT_REFRESH_SECRET=""

# Providus Bank — fill in once API docs are shared
PROVIDUS_BASE_URL=""
PROVIDUS_API_KEY=""
PROVIDUS_WEBHOOK_SECRET=""

# AWS (used by API for S3 receipt uploads — loan repayment manual proofs)
AWS_REGION="us-east-1"
AWS_S3_BUCKET_RECEIPTS=""

# Xpress Wallet — card issuance (virtual + physical). Fill in once API docs are shared.
XPRESS_WALLET_BASE_URL="https://payment.xpress-wallet.com/api/v1"
XPRESS_WALLET_API_KEY=""
XPRESS_WALLET_WEBHOOK_SECRET=""

# ATM card fees — flat amount in NGN charged to PersonalAccount on admin approval.
# Same "open decision, currently flat" status as the withdrawal fee — see README.
CARD_FEE_VIRTUAL="500"
CARD_FEE_PHYSICAL="2000"
